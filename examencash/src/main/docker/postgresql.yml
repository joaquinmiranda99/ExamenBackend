version: '2'
services:
  examencash-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/examencash/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=examencash
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
